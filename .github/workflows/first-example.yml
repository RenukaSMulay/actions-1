name: My First WorkFlow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My first Github Actions Job"

            - name: Lsit files
              run: ls

            - name: Read file
              run: cat README.md
              